apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: eastwest-gateway
  namespace: istio-system
spec:
  gatewayClassName: istio
  listeners:
  - name: tls-crosscluster
    port: 15443
    protocol: TLS
    tls:
      mode: Passthrough